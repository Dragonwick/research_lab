# Example workflow for ot2_color_lab
#
# This is a minimal workflow to demonstrate the workflow structure.
# Replace with your actual experiment steps.

name: example_workflow
description: "An example workflow for ot2_color_lab"

# Workflow parameters (passed in when starting the workflow)
parameters:
  - name: message
    type: string
    description: "Message to pass to the action"
    default: "Hello from ot2_color_lab!"

# Workflow steps
# Uncomment and modify once you have nodes configured
# steps:
#   - name: step_1
#     description: "Execute an action on a node"
#     node: my_node
#     action: example_action
#     args:
#       message: "${parameters.message}"

steps: []

# Metadata
metadata:
  author: "MADSci User"
  version: "1.0.0"
  tags:
    - example
    - generated
